# Rejected initial dataset

Do not use these `train.jsonl` and `validation.jsonl` files for SFT. Their rule-based insertion of generic Mario phrases does not meet the intended persona requirement. They remain here only to make the previous experiment and its checks traceable until a reviewed replacement is ready.

The integrity reports establish preservation of the original math text, not persona quality. See `MARIO_STYLE_PREVIEW.md` for genuine context-aware rewrite examples and `prompts/mario_rewrite.md` for the replacement specification. The full 3,500-example replacement has not yet been generated.
